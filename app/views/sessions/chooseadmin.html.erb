<h1>Log in as admin?</h1>
<%= form_tag(sessions_setadmin_path) do %>
  <div>
  <%= check_box_tag(:yes) %>
  <%= label_tag(:yes, "Yes") %>
  <%= check_box_tag(:no) %>
  <%= label_tag(:no, "No") %>
  </div>
  <div>
    <input type="submit" value="Submit">
  </div>
<% end %>